export enum ScenesActionType {
	NEXT_SCENE = "next",
	PREVIOUS_SCENE = "previous",
}

export enum ScenesType {
	OPTION = "option",
	FORM = "form",
}

export enum API {
	SHELTERS = "shelters_api",
	PREFIXER = "prefix",
}
